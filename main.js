

export function rainbowColour() {

  return  {rainbowString,
    originalLength,
    finalLength,
  }
};


//return this function as a string that excludes the colour indigo because that is not a colour of the rainbow
